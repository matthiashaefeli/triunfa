<nav class="navbar navbar-default navbar-fixed-top text-uppercase">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag("logo.png", size: "75x45", alt: "BWTX", :class => "d-inline-block align-top img-nav img-nav-mo")%>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/">Pagina de inicio</a></li>

        <!-- <li><a href="">uno</a></li> -->
        <!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="">dos</a></li>
            <li><a href="">tres</a></li>
            <li><a href="">quatro</a></li>
            <li><a href="">cinco</a></li>
          </ul> -->
        <!-- </li>
        <li><a href="">seis</a></li>
        <li><a href="">siete</a></li>
        <li><a href="">ocho</a></li> -->

          <!-- nav for all -->
        <% if !current_user %>
          <li><a href="/#courselink">Cursos</a></li>
          <li><a href="/#linkslink">Links</a></li>
          <li><%= link_to "Entrar", new_user_session_path %></li>
        <% else %>
          <% searched_user = check_user(current_user) %>

          <!-- nav for admin -->
          <% if searched_user == Admin %>
            <li><%= link_to "Cursos", courses_path %></li>
            <li><%= link_to "Instructores/Admin", teachers_path %></li>
            <li><%= link_to "Grupos", groups_path %></li>
            <li><%= link_to "Estudiantes", students_path %></li>
            <li><%= link_to "Estatisticas", statistics_path %></li>
            <li><%= link_to "biblioteca", libraries_path %></li>

            <!-- nav for teachers -->
          <% elsif searched_user == Teacher %>
            <% if (Teacher.find_by(user: current_user)).activ %>
              <li><%= link_to "biblioteca", libraries_path %></li>
              <li><%= link_to "Instructores", teachers_path %></li>
              <li><%= link_to "Grupos", rooms_path %></li>
              <% if current_user.avatar_file_name == nil %>
                <li><%= link_to current_user.name, edituser_path(current_user.id) %></li>
              <% else %>
                <li><%= link_to (image_tag current_user.avatar.url(:thumb), :class => "user-avatar"), edituser_path(current_user.id)  %></li>
              <% end %>
            <% end %>

            <!-- nav for students -->
            <% elsif searched_user == Student %>
              <% if current_user.avatar_file_name == nil %>
                <li><%= link_to current_user.name, edituser_path(current_user.id) %></li>
              <% else %>
                <li><%= link_to (image_tag current_user.avatar.url(:thumb), :class => "user-avatar"), edituser_path(current_user.id)  %></li>
              <% end %>
              <li><%= link_to "Tareas", tasks_path(group: Student.find_by(user: current_user).group) %></li>
              <li><%= link_to "Foro", room_path(Room.find_by(group: Student.find_by(user: current_user).group.id)) %></li>

            <% else %>
          <% end %>

          <!-- logout for all -->
          <li><%= link_to "Salida", welcome_path(current_user.id), method:"delete" %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>