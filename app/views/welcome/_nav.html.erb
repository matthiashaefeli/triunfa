<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/"><%= image_tag("logo.png", size: "50x30", alt: "BWTX", :class => "d-inline-block align-top img-nav img-nav-mo")%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li><a href="/">Pagina de inicio</a></li>
        <!-- <li><a href="">uno</a></li> -->
        <!-- <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="">dos</a></li>
            <li><a href="">tres</a></li>
            <li><a href="">quatro</a></li>
            <li><a href="">cinco</a></li>
          </ul> -->
        <!-- </li>
        <li><a href="">seis</a></li>
        <li><a href="">siete</a></li>
        <li><a href="">ocho</a></li> -->

          <!-- nav for all -->
        <% if !current_user %>
          <li><%= link_to "Entrar", new_user_session_path %></li>
        <% else %>
          <% searched_user = check_user(current_user) %>

          <!-- nav for admin -->
          <% if searched_user == "admin" %>
            <li><%= link_to "Cursos", courses_path %></li>
            <li><%= link_to "Grupos", groups_path %></li>
            <li><%= link_to "Facilitadores", teachers_path %></li>

            <!-- nav for teachers -->
          <% elsif searched_user == "teacher" %>
            <li><%= link_to "Grupos", groups_path %></li>
            <% if  (Teacher.find_by(user: current_user)).avatar_file_name == nil %>
              <li><%= link_to current_user.name, edit_teacher_path(Teacher.find_by(user: current_user.id)) %></li>
            <% else %>
              <li><%= link_to (image_tag (Teacher.find_by(user: current_user)).avatar.url(:thumb), :class => "user-avatar") %></li>
            <% end %>

            <!-- nav for students -->
          <% elsif searched_user == "student" %>
            <% if  (Student.find_by(user: current_user)).avatar_file_name == nil %>
              <li><%= link_to current_user.name, edit_student_path(Student.find_by(user: current_user.id)) %></li>
            <% else %>
              <li><%= link_to image_tag (Student.find_by(user: current_user)).avatar.url(:thumb), :class => "user-avatar" %></li>
            <% end %>
          <% else %>
          <% end %>

          <!-- logout for all -->
          <li><%= link_to "Salida", destroy_user_session_path, method:"delete" %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>