
<% if !current_user %>
<div id="index-home">
    <div class="programa">
        <div class="programa-child">
            <h4>Programa educativo de<br>Superate y Triunfa</h4>
            <p>
            Una experiencia de aprendizaje práctica<br> que
            enseña Aprendizaje Digital y Alfabetización Financiera
            en una sala de computación siguiendo nuestro
            plan de estudios amigable.
            </p>
        </div>
    </div>

</div>
<div id="courses-title">
    <h1>Cursos</h1>
</div>
<div id="courses">
    <div class="items-courses"><%= link_to image_tag("word.png", :class => "course-img"), new_user_session_path %></div>
    <div class="items-courses"><%= link_to image_tag("excel.png", :class => "course-img"), new_user_session_path %></div>
    <div class="items-courses"><%= link_to image_tag("power.png", :class => "course-img"), new_user_session_path %></div>
</div>
<div id="courses-title">
    <h1>Links</h1>
</div>
<div id="courses">
    <div class="items-courses"><%= link_to image_tag("juntos.jpg", :class => "course-img"), "http://www.juntospodemos.org/", :target => "_blank" %></div>
    <div class="items-courses"><%= link_to image_tag("parents.png", :class => "course-img"), "https://www.parentsalliance.org/", :target => "_blank"  %></div>
</div>

<% else %>
<div>
    <% if check_user(current_user) != "new_user" && !online.online %>
        <% update_online(check_user(current_user)) %>
    <% end %>

    <div id="online_users">
    <% all_users.each do |user| %>
        <p id="online<%= user.user.id %>"><%= user.user.name %> <%= user.user.lastname %></p>
    <% end %>
    </div>

    <% searched_user = check_user(current_user) %>

    <% if searched_user == "new_user" %>
        <%= render "./students/new.html.erb" %>

    <% elsif searched_user == Admin %>
        <p>Forums de los grupos</p>
        <% @rooms.each do |r| %>
            <p><%= link_to r.group.name, room_path(r.id) %> <%=  r.group.key %></p>
        <% end %>
        <%= render "chat.html.erb" %>

    <% elsif searched_user == Student %>
        <p>Forum de tu grupo</p>
        <%= link_to Student.find_by(user: current_user).group.name, room_path(Room.find_by(group: Student.find_by(user: current_user).group.id)) %>
        <%= render "chat.html.erb" %>

    <% elsif searched_user == Teacher %>
        <p>Forums de los grupos</p>
        <% @teacher_rooms.each do |r| %>
            <p><%= link_to r.group.name, room_path(r.id) %> <%=  r.group.key %></p>
        <% end %>
        <%= render "chat.html.erb" %>
    <% end %>
</div>
<% end %>



    