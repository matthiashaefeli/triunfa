<div>
    <h1><%= @course.name %></h1>
    <p>Fecha Inicio: <%= @course.start %></p>
    <p>Fecha Final: <%= @course.end %></p>
    <p><%= link_to "Editar Curso", edit_course_path(@course.id) %></p>
    <li><%= link_to image_tag @course.avatar.url(:thumb), :class => "course-avatar" %></li>
    <!-- <p><%= link_to "Borrar Curso", course_path(@course.id), method: "delete" %></p> -->
</div>