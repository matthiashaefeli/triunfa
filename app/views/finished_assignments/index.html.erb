
<div class="finished-assig under-nav">
  <% if @language %>
    <p><%= link_to 'Regresar', root_path, class: "btn btn-sm back-button" %></p>
    <h4>Todos mis Cursos graduados:</h4>
    <ul>
        <% points_per_course.each do |group| %>
            <li><%= group.course.name %></li>
        <% end %>
    </ol>
  <% else %>
    <p><%= link_to 'Back', root_path, class: "btn btn-sm back-button" %></p>
    <h4>All my graduate courses:</h4>
    <ul>
        <% points_per_course.each do |group| %>
            <li><%= group.course.name %></li>
        <% end %>
    </ol>
  <% end %>
</div>