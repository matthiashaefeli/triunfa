<div><%= @room.group.name %></div>
<%= form_for :message, url: messages_path do |f| %>
  <%= f.label :text %>
  <%= f.text_area :body, id: "message-body" %>
  <%= hidden_field_tag "room", @room.id  %>
  <br>
  <p>
    <%= f.label :adjuntar_imagen %>
    <%= f.file_field :avatar %>
  </p>
  <%= f.submit "Enviar Mensaje" %>
<% end %>
<%= render "messages/messages" %>
